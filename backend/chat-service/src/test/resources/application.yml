spring:
  jpa:
    # Hibernate가 엔티티 기반 테이블(chat_rooms, chat_messages)을 먼저 만든 뒤에 schema.sql이
    # 추가 테이블(posts)을 만들도록 순서를 맞춘다. findMyRooms 네이티브 쿼리가 같은 DB의 posts
    # 테이블을 조회하는데, 테스트용 H2에는 post-service의 Post 엔티티가 없어 직접 만들어줘야 한다.
    defer-datasource-initialization: true
